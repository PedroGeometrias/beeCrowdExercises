// CODE BY pedroGeometrias
// ID -> 1005
#include <stdio.h>
int main(void) {
  double A, B;
  scanf("%lf %lf", &A, &B);
  A*=3.5;
  B*=7.5;
  double MEDIA = (A+B)/11;
  printf("MEDIA = %.5lf\n", MEDIA);
  return 0;
}
